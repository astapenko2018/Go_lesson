package main

import (
	"bufio"
	"fmt"
	"os"
	"strconv"
	"strings"
)

func insertionSort(origSlice [] int) []int {
	lenSlice := len(origSlice)
	res := make([]int, lenSlice)
	copy(res, origSlice)

	for i := 1, i < lenSlice; i ++{
		fpo
	}
}